reflex>=0.6.0
supabase>=2.7.0
httpx>=0.27.0
pypdf>=4.2.0
python-docx>=1.1.0
python-dotenv>=1.0.0
stripe>=9.0.0
razorpay>=1.4.0
# Reflex's backend ASGI app is Starlette, not FastAPI (verified against the
# installed version) — FastAPI is used only for the webhook routes, mounted
# as a sub-app onto Reflex's escape hatch. See webapp/webapp/webapp.py.
fastapi>=0.110.0
